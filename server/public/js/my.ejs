<%
    capitalize= function(string) {
        return string.replace(/^\w/, (c) => c.toUpperCase());
    }

    withoutLast5 = function(str){
        console.log(str.slice(0, -5));
       return str.slice(0, -5);
    }
%>